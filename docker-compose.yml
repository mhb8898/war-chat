services:
  war-chat:
    build: .
    container_name: war-chat
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - DATA_DIR=/data
      - REQUIRE_INVITE=true
    volumes:
      - ./data:/data
    healthcheck:
      test: ["CMD", "/war-chat", "-healthcheck"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 5s
